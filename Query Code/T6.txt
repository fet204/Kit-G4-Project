SELECT 
    total,
    ppl
FROM( 

    SELECT 
        userid, SUM(amount) as total,
        userid as ppl
    FROM 
        donation
    GROUP BY
        userid
) as fullTotal
Where 
    total > 20;
